# M29AZ next-chat handoff

M29AY hardware testing exposed a deterministic MissionDB crash in all three campaigns. Three Vita cores normalize to M29AY ELF PC `0x815f5372`, a weak recursive `strcasecmp()` generated by the interaction of the global `stricmp -> strcasecmp` portability macro and the `__LINUX` helper in `ltbasedefs.h`.

M29AZ fixes this before forced includes: `stringmgr.cpp` is compiled with `FEARVITA_NO_MSVC_STRICMP_ALIASES=1`. Its object now has only `U strcasecmp`; final ELF uses the real strong libc function and no self-call.

Artifacts/hashes are in `CURRENT_STATE_M29AZ.txt` and `M29AZ_CRASH_ANALYSIS_2026-09-12.md`.

Next action is hardware retest. If MissionDB now completes, continue linker/runtime debugging from the first later log/core frontier. Do not fake GS_PLAYING or world load.
