name: KeeperFX Vita Known-Good CI

on:
  push:
    branches: [ "keeperfx-vita-known-good" ]
  workflow_dispatch:

jobs:
  build-vita:
    name: Build known-good KeeperFX PS Vita
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/cerwym/build-vitasdk:latest
      options: --user root

    steps:
      - name: Checkout Vita helpers
        uses: actions/checkout@v4
        with:
          ref: keeperfx-vita-known-good
          path: ci

      - name: Checkout KeeperFX known-good Vita commit
        uses: actions/checkout@v4
        with:
          repository: cerwym/keeperfx
          ref: 466e0753e17cee876f2ace6fa6b1e17f3ad44b0d
          fetch-depth: 1
          submodules: recursive
          path: keeperfx

      - name: Mark git directory safe
        run: git config --global --add safe.directory '*'

      - name: Install touch-first Vita controls
        working-directory: keeperfx
        run: |
          cp ../ci/keeperfx-vita/input_vita_touch.c src/input/input_vita.c
          cp ../ci/keeperfx-vita/WindowSystemVita_touch.cpp src/platform/WindowSystemVita.cpp
          python3 - <<'PY'
          from pathlib import Path
          p = Path('src/platform/WindowSystemVita.cpp')
          s = p.read_text(encoding='utf-8')
          s = s.replace('lbMouse.MMouseX', 'lbDisplay.MMouseX')
          s = s.replace('lbMouse.MMouseY', 'lbDisplay.MMouseY')
          p.write_text(s, encoding='utf-8')
          PY

      - name: Install first-run data bootstrap and Deeper Dungeons launch path
        working-directory: keeperfx
        run: |
          cp ../ci/keeperfx-vita/vita_bootstrap.hpp src/platform/vita_bootstrap.hpp
          python3 - <<'PY'
          from pathlib import Path

          p = Path('src/platform/PlatformHomebrewMain.cpp')
          s = p.read_text(encoding='utf-8')
          old = '#include "platform/PlatformVita.h"\n'
          new = old + '#include "platform/vita_bootstrap.hpp"\n'
          if old not in s:
              raise SystemExit('PlatformHomebrewMain include anchor not found')
          s = s.replace(old, new, 1)

          old = '''int main(int argc, char* argv[]) {
#if defined(PLATFORM_VITA)
    // Create log file before SystemInit so the boot-log writes inside have a file to append to.
    { FILE* _f = fopen("ux0:data/keeperfx/kfx_boot.log", "w"); if (_f) { fprintf(_f, "main-enter\\n"); fclose(_f); } }
#endif
'''
          new = '''int main(int argc, char* argv[]) {
#if defined(PLATFORM_VITA)
    // Ensure the runtime root exists before PlatformVita::SystemInit() chdirs into it.
    kfx_vita_bootstrap::ensure_data_root();
    // Create log file before SystemInit so the boot-log writes inside have a file to append to.
    { FILE* _f = fopen("ux0:data/keeperfx/kfx_boot.log", "w"); if (_f) { fprintf(_f, "main-enter\\n"); fclose(_f); } }
#endif
'''
          if old not in s:
              raise SystemExit('PlatformHomebrewMain main anchor not found')
          s = s.replace(old, new, 1)

          old = '''    PlatformManager::Get()->SystemInit();
    KfxMemInit();

    // Route SDL log output to a file before SDL_Init is called in VideoInit.
'''
          new = '''    PlatformManager::Get()->SystemInit();
    KfxMemInit();

#if defined(PLATFORM_VITA)
    // Install the VPK-bundled runtime data once, before KeeperFX opens any assets.
    // Completed files are skipped on retry; the marker is written only after full success.
    if (!kfx_vita_bootstrap::install_bundled_data()) {
        kfx_vita_bootstrap::shutdown_apputil();
        return 24;
    }
    const bool vita_deeper_launch = kfx_vita_bootstrap::deeper_requested();
#endif

    // Route SDL log output to a file before SDL_Init is called in VideoInit.
'''
          if old not in s:
              raise SystemExit('PlatformHomebrewMain bootstrap anchor not found')
          s = s.replace(old, new, 1)

          old = '    return kfxmain(argc, argv);\n'
          new = '''#if defined(PLATFORM_VITA)
    int result = kfx_vita_bootstrap::run_kfxmain(argc, argv, vita_deeper_launch);
    kfx_vita_bootstrap::shutdown_apputil();
    return result;
#else
    return kfxmain(argc, argv);
#endif
'''
          if old not in s:
              raise SystemExit('PlatformHomebrewMain kfxmain anchor not found')
          s = s.replace(old, new, 1)
          p.write_text(s, encoding='utf-8')

          p = Path('build/cmake/modules/PlatformVita.cmake')
          s = p.read_text(encoding='utf-8')
          old = 'ScePower_stub SceAudio_stub SceShaccCg_stub SceKernelDmacMgr_stub)'
          new = 'ScePower_stub SceAudio_stub SceShaccCg_stub SceKernelDmacMgr_stub SceAppUtil_stub)'
          if old not in s:
              raise SystemExit('PlatformVita.cmake stub anchor not found')
          s = s.replace(old, new, 1)
          p.write_text(s, encoding='utf-8')
          PY

      - name: Cache FFmpeg-vita
        id: cache-ffmpeg-vita
        uses: actions/cache@v4
        with:
          path: |
            /usr/local/vitasdk/arm-vita-eabi/lib/libavformat.a
            /usr/local/vitasdk/arm-vita-eabi/lib/libavcodec.a
            /usr/local/vitasdk/arm-vita-eabi/lib/libavutil.a
            /usr/local/vitasdk/arm-vita-eabi/lib/libswresample.a
            /usr/local/vitasdk/arm-vita-eabi/include/libavformat
            /usr/local/vitasdk/arm-vita-eabi/include/libavcodec
            /usr/local/vitasdk/arm-vita-eabi/include/libavutil
            /usr/local/vitasdk/arm-vita-eabi/include/libswresample
          key: ffmpeg-vita-fish47-v3

      - name: Build FFmpeg-vita
        if: steps.cache-ffmpeg-vita.outputs.cache-hit != 'true'
        run: |
          rm -rf /tmp/FFmpeg-vita
          git clone --depth 1 https://github.com/fish47/FFmpeg-vita.git /tmp/FFmpeg-vita
          cd /tmp/FFmpeg-vita
          curl -fsSL https://raw.githubusercontent.com/FFmpeg/FFmpeg/n6.0.1/libavformat/smacker.c -o libavformat/smacker.c
          curl -fsSL https://raw.githubusercontent.com/FFmpeg/FFmpeg/n6.0.1/libavcodec/smacker.c -o libavcodec/smacker.c
          ./configure \
            --target-os=vita --enable-vita --disable-shared --disable-programs --disable-doc \
            --disable-debug --enable-static --enable-small --prefix="$VITASDK/arm-vita-eabi" \
            --disable-everything \
            --enable-decoder=aac_vita,mp3_vita,aac,adpcm_ima_qt,alac,flac,mp3,opus,pcm_s16le,pcm_s24le,pcm_s32le,vorbis,smacker,smackaud \
            --enable-demuxer=aac,mp3,mp4,m4a,matroska,flac,ogg,opus,wav,smacker \
            --enable-muxer=null --enable-parser=aac,mpegaudio,vorbis,opus \
            --enable-protocol=file --disable-network --disable-bzlib
          make -j$(nproc)
          make install

      - name: Install vitaGL dependencies
        run: $VITASDK/bin/vdpm vitashark mathneon

      - name: Configure known-good Vita release
        working-directory: keeperfx
        run: cmake --preset vita-release

      - name: Build known-good Touch VPK
        working-directory: keeperfx
        shell: bash
        run: |
          set +e
          cmake --build --preset vita-release > build.log 2>&1
          status=$?
          tail -n 160 build.log
          if [ "$status" -ne 0 ]; then
            echo '=== ERROR DIAGNOSTICS ==='
            grep -E -C 4 'undefined reference|collect2:|(^|[[:space:]])ld:|error:' build.log | tail -n 300 || true
          fi
          exit "$status"

      - name: Upload known-good Touch VPK
        uses: actions/upload-artifact@v4
        with:
          name: keeperfx-vita-touch-known-good
          path: |
            keeperfx/out/build/vita-release/*.vpk
            keeperfx/out/build/vita-release/*.elf
          if-no-files-found: error
